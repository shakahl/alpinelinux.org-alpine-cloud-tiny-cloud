#!/sbin/openrc-run
# vim:set ft=sh:
# shellcheck shell=sh

description="Tiny Cloud Bootstrap - net phase"
depend() {
	need net
	before tiny-cloud
}

start() {
	ebegin "Tiny Cloud - net"
	tiny-cloud net
	eend $?
}
